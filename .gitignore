# Segredos — NUNCA versionar
.env
*.env
!.env.example
.credentials.json
bw-jwt.txt

# Vercel / build
.vercel/
lp/.vercel/
node_modules/
*.local

# Firecrawl — dumps brutos intermediários
marca/acervo/**/_raw.json
lp/**/_raw.json

# Referência de design (input do /marca, pesada, regenerável — não runtime)
marca/referencia/

# Cache do quality gate (regenerável)
.impeccable/

# Temp / OS / editor
*.tmp
_verify_tmp.html
.DS_Store
Thumbs.db
*.err
